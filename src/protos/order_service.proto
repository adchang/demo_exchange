syntax = "proto3";

import "order.proto";

package erx.api.order;
option csharp_namespace = "DemoExchange.Api.Order";

message StringData {
  string data = 1;
}

service OrderService {
  rpc GetQuote(QuoteRequest) returns (Quote);
  rpc Echo(StringData) returns (StringData);
}
